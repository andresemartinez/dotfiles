
" Necesary for lots of cool vim things
set nocompatible

" Line numbers
:set number

" Tab handling
set expandtab
set tabstop=4
set softtabstop=4
set shiftwidth=4

" Search down into subfoulders
" Provides tab-completion for all file-realted tasks
set path+=**

" Displays all matching files when we tab complete
set wildmenu
